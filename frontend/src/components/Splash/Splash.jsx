const Splash = props => {
    const [showRegister, setShowRegister] = useState(false)
    return (
        <h2>Splash</h2>
        [showRegister && (
            
        )]
    )
}
export default Splash